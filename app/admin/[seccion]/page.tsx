export default async function AdminSection({params}:{params:Promise<{seccion:string}>}){const {seccion}=await params; return <><p className="eyebrow">ADMINISTRACIÓN</p><h1 className="display mt-3 text-5xl capitalize">{seccion}</h1><div className="mt-10 rounded-lg bg-white p-7 text-sm text-slate-500 shadow-sm">Esta sección está preparada para conectarse a Supabase y gestionar contenido publicado.</div></>}
