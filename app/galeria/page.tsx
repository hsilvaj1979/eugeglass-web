import { PageHero } from "@/components/page-hero"; import { SiteFooter } from "@/components/site-footer"; import { SiteHeader } from "@/components/site-header";
export default function Galeria(){return <><SiteHeader/><PageHero eyebrow="GALERÍA" title="Detalles que hacen la diferencia." copy="Una colección visual de referencias para imaginar las posibilidades del vidrio."/><main className="container columns-1 gap-5 py-20 md:columns-3">{Array.from({length:9}).map((_,i)=><div key={i} className="image-card mb-5 break-inside-avoid" style={{height:`${220+(i%3)*85}px`}} aria-label={`Proyecto de referencia ${i+1}`}/>)}</main><SiteFooter/></>}
